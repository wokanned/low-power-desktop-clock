# 嵌软开发规范

适用范围：中小型嵌入式软件项目  
适用对象：团队内部开发成员  
默认工作流：CubeMX 生成框架 + VS Code 修改与审查代码 + Keil 构建、下载、观察板上现象

## 正文

### 1. 文档定位

这份文档是项目级主规范，不是单个器件驱动实现细则。

它主要回答四类问题。

- 工程目录怎么组织。
- 各层代码放在哪一层。
- 各层之间允许依赖什么，不允许依赖什么。
- 基于 CubeMX 的分层模板工程，在 VS Code 改代码、在 Keil 构建下载和观察现象的工作流下，用户代码怎么长期维护和复用。编辑器错误检查和可选的 CMake 快速检查作为次验证路径。

相关文档入口：

- 单器件驱动实现细则位于 `docs/ic开发规范.md`。该文档依附于本文件存在。未特别说明时，以本文件为准。
- AI 自动加载规则入口位于 `.github/copilot-instructions.md` 与 `.github/instructions/*.instructions.md`。
- 若使用不支持自动加载仓库规则的 AI 或 CLI，应显式读取本文件。任务涉及寄存器、初始化时序、恢复或总线协议细节时，再补读 `docs/ic开发规范.md`。

嵌软开发规范管整个项目怎么搭架子。  
IC 开发规范只补单个器件接入和实现细则。

### 2. 阅读前建议

建议先有三个基本认识。

1. 工程分层意识。应用层、器件驱动层、平台适配层、中间件层，各自处理不同问题。
2. 接口抽象意识。先定义能力接口，再做平台绑定。不要在驱动模块内部写死具体 HAL 句柄。
3. 依赖方向意识。上层依赖抽象接口。不要反向依赖底层实现。变化尽量停在平台适配层。

    这样做有三个直接好处。

    - 便于复用已经写好的驱动和中间件代码。
    - 换板、换 MCU、换器件时，改动范围更小。
    - 项目后期不容易堆成难维护的结构。

### 2.1 项目起步卡

开始新建工程、大改工程、接新模块，或让 AI 深度参与前，先确认这些事实。

- MCU 或板卡型号。
- 当前主构建路径。
- 当前主验证路径。
- 生成代码边界和 `USER CODE` 入口。
- 是否同时维护 `MDK-ARM/`。
- 本次任务对应的文档入口。

若其中某项还不明确，先查仓库文档、现有代码和构建配置，再继续修改。

### 3. 项目组织原则

#### 3.1 成熟项目的共性

参考 Zephyr、ESP-IDF 等公开项目，可以先抓住这些共性。

1. 项目入口明确。顶层目录会分开应用代码、平台代码、构建配置和生成产物。
2. 配置与源码分离。配置文件、构建文件、源码文件不混堆。
3. 驱动与业务分离。业务层不直接写寄存器。驱动层不直接混入业务逻辑。
4. 构建产物独立。编译输出目录和源码目录分开，仓库更干净。
5. 平台相关代码集中。板卡、MCU、HAL 强相关的内容，集中放在平台层或生成代码层。
6. 模块边界清楚。一个模块的公开接口、私有实现、依赖关系都能看明白。

#### 3.2 对本团队的落地结论

当前模板工程以 CubeMX 生成框架、VS Code 修改代码、Keil 构建下载和现象观察为主。编辑器错误检查和可选的 CMake 快速检查作为次验证路径。  
所以这里不照搬大框架的全部构建体系，只吸收真正有用的部分。

- 目录按职责分层，不按谁先写出来来堆文件。
- 生成代码和用户代码分开。
- 驱动、平台适配、应用逻辑分开维护。
- 编译输出、工程文件、文档资料单独管理。

#### 3.3 驱动复用的基本路径

可复用驱动代码的核心思路，可以先按三步看。

1. 去耦模块驱动。把器件驱动里依赖具体 MCU 和 HAL 的部分抽成接口。驱动主体只保留寄存器协议、状态机和数据处理。
2. 平台实现接口。在具体平台上实现 SPI、I2C、UART、GPIO、delay、tick 等能力，再和驱动接口绑定。
3. 上层只调能力接口。业务代码只调用模块提供的能力接口，不依赖具体硬件细节。

这样做的目标很直接。

- 换 MCU 时，主要改平台层。
- 换器件时，尽量不改业务层。
- 后续优化和加功能时，不破坏原有结构。

### 4. 推荐目录结构

对于本团队当前模板工程，推荐使用如下结构。

```text
Project_Root/
├── Core/                   # CubeMX 生成代码（启动、时钟、中断、HAL初始化）
├── app/                    # 应用层（初始化编排、任务循环、业务流程）
├── device/                 # 器件驱动主体（寄存器协议、状态机、数据转换）
├── middleware/             # 中间件（协议、算法、日志、存储、数据处理）
├── platform/               # 平台适配层（SPI/I2C/UART/GPIO/delay/tick 绑定）
├── docs/                   # 项目文档（规范、说明、迁移清单、联调记录）
├── build/                  # 可选：CMake/Ninja 等附加构建产物
├── MDK-ARM/                # 当前主构建、下载、调试工程文件
├── *.ioc                   # CubeMX 工程配置
├── startup_xxx.s           # 启动文件
└── STM32xxxx_FLASH.ld      # 链接脚本
```

这些工具链配套文件不属于业务层本身。  
但它们必须纳入工程管理。

### 5. 各目录职责

#### 5.1 Core

- 由 CubeMX 生成并维护。
- 主要包含时钟配置、外设初始化、中断入口、系统启动文件。
- 用户在该层只保留必要调用入口，不在这里堆业务逻辑。

#### 5.2 app

- 放项目业务逻辑。
- 负责初始化编排、任务循环、异常恢复、状态上报、调试输出。
- 可以调用 `device`、`middleware`、`platform`，但不应直接操作底层寄存器。

#### 5.3 device

- 放具体器件驱动，如传感器、显示屏、Flash、扩展芯片等。
- 负责寄存器定义、初始化流程、数据读写、状态解析。
- 原则上应尽量平台无关，不直接写死 HAL 句柄。

#### 5.4 platform

- 放平台适配代码。
- 负责把 `device` 层需要的 SPI、I2C、UART、GPIO、delay、tick 等能力绑定到当前 MCU 平台实现。
- 允许依赖 HAL、BSP、板级引脚和中断资源。

#### 5.5 middleware

- 放可复用的通用能力，不放具体器件寄存器驱动。
- 常见内容包括通信协议、算法、滤波、日志、存储封装、状态机。
- 中间件可以被多个 `app` 模块复用。

#### 5.6 docs

- 放项目说明、开发规范、接口文档、联调记录、问题复盘。
- 该目录服务于团队协作，不是可有可无的附件目录。

#### 5.7 build / MDK-ARM

- `build/` 若后续启用 CMake 或其他附加构建路径，可放其输出、缓存和日志等自动生成产物。
- `MDK-ARM/` 当前放 Keil 工程文件、下载配置、调试配置，是主构建、下载和板级现象观察入口。
- 两者都属于工具链资产。当前仓库先以 `MDK-ARM/` 为主；若后续补回 CMake，再补充对应构建入口说明。

#### 5.8 文件命名约定

命名时先看三个原则。

1. 先保证语义清楚。
2. 再保证简单稳定。
3. 最后再考虑看起来完整。

对本模板工程，推荐采用全小写 `snake_case`。再按层级使用最短可辨识命名。

- `app/` 使用业务主题名，不使用空泛总名。示例：`baro.c`、`imu.c`、`tam.c`、`vofa.c`。
- `device/` 使用 `类型_器件名`。示例：`baro_spl06.c`、`imu_qmi8658a.c`、`tam_qmc5883p.c`。
- `platform/` 使用 `stm32_器件名`。表示这是当前 MCU 平台上的器件绑定实现。示例：`stm32_spl06.c`、`stm32_qmi8658a.c`、`stm32_qmc5883p.c`。
- `middleware/` 使用能力名或算法名。示例：`mahony_ahrs.c`。

补充约束如下。

- `.c` 与 `.h` 必须同名配对。只改一个文件名是不完整修改。
- 不保留大小写混用、下划线风格混用或历史拼写错误。例如 `platfrom` 这类命名应尽快收敛。
- 不为了绝对完整而把文件名拉得过长。只要在本仓库上下文里可唯一辨识即可。
- 若器件名本身已经足够表达职责，可不重复加 `driver`、`module`、`adapter` 等低信息量后缀。
- 若后续更换 MCU 平台，优先替换 `platform/` 文件名中的平台前缀，不改 `device/` 的器件语义名。

#### 5.9 C 语言编写风格草案

这一节只补 C 代码写法。  
它仍然是项目级草案。  
它不替代 `docs/ic开发规范.md` 里的器件实现细则。

##### 5.9.1 适用范围

- 这一节主要面向 C 代码。
- 重点覆盖底层驱动和核心逻辑。
- 其他语言模块可按各自语言习惯处理。但不能破坏本文件定义的分层和接口边界。

##### 5.9.2 命名和类型

- 项目内语言要统一。推荐全英语。也可统一使用拼音。不要混用。
- 函数名在同一目录内保持一种风格。若目录里已用 `snake_case`，新代码继续沿用。
- 变量名要能看懂用途。除了 `i`、`j` 这类短循环变量，不用无意义单字母。
- 全局变量用 `g_` 前缀。静态变量用 `s_` 前缀。指针变量用 `p_` 前缀。
- 整型优先使用 `stdint.h` 的定宽类型。如 `uint8_t`、`int16_t`、`uint32_t`。
- 跨文件共享的数据，优先用 `struct` 或 `enum` 收口。

##### 5.9.3 函数和变量

- 为了便于阅读，也为了兼容旧工具链，局部变量建议放在函数开头集中定义。
- 不要在代码块中间随手插入一串局部变量定义。
- 中断和多任务共享的变量，要按实际需要声明为 `volatile`。
- 单个函数只做一件清楚的事。入口、主逻辑、返回处理尽量分开写。
- 若文件里已经沿用 `} // end of function_name` 这类函数结束注释风格，新改动保持一致。一个文件内不要混成两套写法。

##### 5.9.4 注释

- 函数头注释要写清用途、参数和返回值。
- 文件头注释建议写清文件名和用途。若目录里已有统一模板，可继续写作者和日期。
- 代码段注释重点写为什么这样做。不要重复写显而易见的动作。

##### 5.9.5 返回值、错误码、内存

- 对外接口的返回值含义要统一。头文件注释里要写清成功值和失败值。
- 能集中定义错误码时，尽量集中定义。不要把一堆临时返回值散在各处。
- 慎用 `malloc` 和 `free`。能用静态数组、对象池或静态创建接口时，优先用静态方案。

##### 5.9.6 格式和可读性

- 一个逻辑块写完再留空行。不要把整个函数写成一整屏。
- 条件分支和循环体要有稳定缩进。括号风格在同一文件内保持一致。
- 魔数不要散在流程代码里。常量优先收成宏、枚举或常量表。
- 复制代码前先判断是否能提成小函数、公共宏或公共表。

### 6. 分层边界

推荐依赖方向如下。

- `app` → `middleware` / `device` / `platform`
- `middleware` → `device` 或公共接口
- `device` → 抽象接口
- `platform` → HAL / BSP / Core 资源
- `Core` → 只负责系统启动和外设初始化，不承载业务决策

也可以直接这样理解。

- `app` 管业务。
- `middleware` 管可复用能力。
- `device` 管器件逻辑。
- `platform` 管平台实现。
- `Core` 管启动和生成代码。

### 7. 强制边界规则

#### 7.1 app 层

- 不直接调用寄存器定义。
- 不直接实现 SPI、I2C、UART 底层读写。
- 不把大段硬件初始化流程直接写进业务模块。

#### 7.2 device 层

- 不直接包含 `main.h`、`spi.h`、`usart.h` 等平台头文件。
- 不直接调用 `HAL_` 开头函数。
- 只处理器件协议、寄存器操作、配置和数据解析。

#### 7.3 platform 层

- 可以依赖 HAL 和板级资源。
- 不承担业务判断。例如告警策略、容错策略、上报策略。

#### 7.4 middleware 层

- 只放协议、算法、日志、存储、模型等可复用能力。
- 不直接承载具体器件寄存器协议。
- 不应夹带具体板级 GPIO、片选、串口实例等平台实现细节。

#### 7.5 Core 层

- 仅作为系统入口和生成代码承载层。
- 用户逻辑尽量写在 `USER CODE` 区。复杂逻辑写到 `app`、`middleware`、`device`、`platform` 中。

### 8. 基于 CubeMX、VS Code、Keil 的开发约束

#### 8.1 VS Code 修改代码约束

- 日常代码编写、重构、文档维护和错误检查，优先在 VS Code 中完成。
- 在 VS Code 中修改代码时，必须遵守本规范的分层边界。
- 修改 `Core/` 下的生成文件时，原则上只改 `USER CODE` 区域。复杂逻辑仍应放回 `app`、`device`、`middleware`、`platform`。
- 若在 VS Code 中新增或移动 `.c/.h` 文件，必须同步检查当前有效构建入口。当前主流程下先核对 Keil 工程组和相关 include 配置；若后续补回 CMake，再同步其源文件列表和 include 路径。
- 当前维护 `MDK-ARM/` 时，不能只看 VS Code 里的文件树。源文件是否真正参与编译，以 Keil 工程组为准。
- VS Code 中的错误检查，以及可选的 CMake 快速检查，主要用于尽早发现代码完整性问题。它们不能替代板上下载和现象观察。
- 对硬件下载、在线调试、现象观察、下载算法和调试脚本等强依赖板级工具链的操作，当前以 Keil 流程为准。

#### 8.2 CubeMX 使用约束

- 所有外设初始化优先通过 CubeMX 配置生成。
- 用户新增 include、变量、函数声明、调用入口，优先放在 `USER CODE BEGIN/END` 区域。
- 不要把长业务逻辑直接写入生成文件的自动区。
- 若某外设的中断、DMA 或其他硬件机制未在 CubeMX 中启用，则默认不应优先改成依赖该机制的实现。
- 应优先保持与当前生成配置一致的轮询或已启用路径。
- 若确需改为中断或 DMA 方案，必须先在 CubeMX 或等价生成配置中明确启用，并同步确认 NVIC、回调入口和重生成后的归属边界。

#### 8.3 Keil 构建、下载与板级观察约束

- 当前主工作流程默认通过 Keil 完成编译、下载和板上现象观察。
- 新增或移动 `.c/.h` 文件后，必须同步确认 Keil 工程组、相关 include 配置和目标输出仍然正确。
- 对 FreeRTOS 相关问题，尤其是任务切换、时序、栈、优先级、临界区、中断和同步对象问题，最终判断要回到板上现象、日志和调试观察。不能只停在编辑器错误检查或可选的快速编译。
- 下载配置、调试脚本、目标输出路径要统一。不要每个人各配一套。
- 若仓库后续补回 CMake，它只作为次验证路径或快速检查路径，不替代当前主流程。

#### 8.4 工程重生成约束

- 重新生成 CubeMX 工程后，必须检查业务调用入口是否还在。
- 当前主流程下，还要检查 Keil 工程中的新增模块、启动文件和关键入口是否仍参与编译。
- 检查 `USER CODE` 区是否被误删或迁移。
- 还要检查 CubeMX 生成的信号量、互斥量、队列、函数入口，是否和代码里实际使用的是同一个。
- 修改 CubeMX 后如果没有同步改代码，容易在这里出问题。可能出现死锁，或者无法获取到信号量等问题。
- 这种情况下，编译器可能不报错，链接器可能报错，所以必须额外检查。

#### 8.5 项目级验证策略

- 默认主验证路径是错误检查与静态检查、Keil 编译与下载、最小观测与功能验证。
- 若仓库后续补回 CMake，可把它作为次验证路径或快速检查路径，但不要把它当成当前唯一出口。
- 若当前没有硬件条件，至少完成错误检查、Keil 编译、接入点核对，并把未执行的板级下载与现象验证显式标为待确认。若仓库同时存在 CMake，可额外补一次快速检查。
- 若改动涉及寄存器、初始化时序、回传、异常恢复或发布门控，除本节外，还应按 `docs/ic开发规范.md` 补做器件级验证。
- 若改动涉及 FreeRTOS 任务调度、时序、栈、中断或同步对象，最终判断应回到板上现象、日志和调试观察。

##### 8.5.1 验证顺序

推荐按下面顺序验证。没有必要每次都把整套都跑满，但不能跳过与你本次改动直接相关的那一级。

1. 错误检查与静态检查。先看编辑器报错、明显依赖缺失、类型不匹配和边界错误。
2. 当前主构建路径编译。默认先用 Keil 跑一次编译，确认改动真的进入了当前有效构建。若仓库同时存在 CMake，可补一次快速检查。
3. 接入点核对。若改动触及 `.ioc`、`USER CODE`、Keil 工程组、启动文件、链接脚本或调用入口，检查接入关系是否仍完整。若仓库同时维护 CMake，再补核对其源文件列表。
4. 下载与最小观测。通过 Keil 下载到板上，再用串口、日志、状态灯、上位机或等价手段，先确认系统仍可观测。
5. 功能路径。只验证本次改动直接影响的主功能，不把无关联调项一起混进来。
6. 异常恢复。若改动涉及初始化、通信、状态机、发布逻辑、任务切换或同步对象，至少补一次失败后恢复检查。
7. 测试接口收口。验证完成后，确认临时探测代码不会默认进入正式主流程。

##### 8.5.2 按改动类型选择最小验证动作

下面四个短模板，优先用于日常改动的最小验证。

1. 文档或纯文本改动：通读受影响章节，检查标题、链接、引用入口和术语是否一致。若改到规范入口，再复查 `.github` 中的文档引用是否失真。
2. CubeMX 改动：先核对 `.ioc`、`USER CODE`、调用入口和 Keil 工程中的相关设置，再跑一次 Keil 编译。若仓库同时存在 CMake，可补一次快速检查。
3. `device` 或 `platform` 改动：先跑 Keil 编译与下载，再做最小观测，然后按 `docs/ic开发规范.md` 至少完成默认配置验证和一次异常恢复验证。若仓库同时存在 CMake，可在下载前补做一次快速检查。
4. `app` 发布与恢复逻辑改动：先跑 Keil 编译与下载，再检查启动路径、首包发布、状态上报和恢复路径。若涉及 FreeRTOS，还要观察任务切换、日志、时序或调试输出；若有联调接口，还要确认默认关闭且不形成双主流程。

#### 8.6 联调临时代码收口约束

- 联调阶段允许临时加入探测代码、测试回传代码和观测接口。例如 VOFA 回传、屏幕显示、状态灯、串口诊断输出等。
- 这类代码必须标记为测试或联调路径。
- 问题定位完成后，必须回归单一路径实现，避免测试逻辑长期参与默认主流程。
- 进入正式工程时，这类接口应默认关闭。
- 如后续仍可能复用，优先通过注释保留、条件编译开关或集中测试入口的方式保留，而不是直接删除。
- 即使选择保留测试接口，也应确保其默认不影响正式业务路径，不制造双主流程或隐式依赖。
- 对外发布前，`main.c` 应只保留稳定主流程，避免诊断分支和业务分支并行。

#### 8.7 Git 版本管理约束

- Git 可以只作为本地版本存档工具使用。即使暂时不推送远程，也应通过小步提交保留关键演进节点。
- 必须先读取检查一次 `.gitignore` 文档的状态。
- 仓库默认只跟踪源码、必要配置、文档和为复现构建所必需的资产。
- `build/`、编译中间文件、缓存、日志和编辑器用户态文件，必须通过 `.gitignore` 排除，避免仓库被自动产物污染。
- 第三方库、厂商代码和生成资产是否纳入版本管理，应按项目目标明确选择。
- 若目标是拉取即构建，可保留 `Drivers/`、`Middlewares/`、必要的 `Core/`、启动文件和链接脚本。
- 若目标是仓库轻量，可停止跟踪其中稳定且可再获取的部分。但必须在 `docs/` 中写清来源、版本、获取方式和重生成路径。
- 不得为了缩小仓库而静默删除构建所必需的依赖。凡是移出版本管理的内容，都要先确认团队内其他人能按文档恢复。
- 提交时应优先只纳入与当前任务直接相关的改动。无关的构建产物、缓存文件、测试日志、误触发的大目录变更，不应混入本次提交。
- 若某个大目录已经被 Git 跟踪，但后续决定改为忽略，应先更新 `.gitignore`，再通过保留本地文件的方式把它从索引中移除，而不是直接删除工作区内容。
- 首次初始化仓库或第一次推送前，应先在 VS Code 源代码管理视图或等价的 `git status` 中核对实际纳入版本管理的文件集合，再执行提交和推送。

#### 8.8 新建工程起步阶段的人侧协作约束

- 如果准备让 AI 参与新建工程或大改工程，先由人尽量搜索并确认最基本事实。包括 MCU 或板卡、总线资源、目标器件、是否裸机或 RTOS、主构建路径、下载调试方式、当前目录结构和已有文档入口。
- 若任务涉及寄存器、初始化时序、总线附加位、功耗模式、板级连线或下载流程，优先查手册、现有工程、仓库文档或经过验证的参考资料。
- 不要把未确认事实直接交给 AI 当成确定条件。
- 给 AI 下任务时，尽量把目标、边界、验证方式和不允许改的区域一起说清楚，不要只给功能目标。
- 新建工程早期，优先让 AI 先输出目录拆分、分层方案、构建接入点和最小验证步骤。确认方案合理后，再让它逐段写代码。
- 对 CubeMX 生成文件、`.ioc`、当前维护的 Keil 工程文件，以及仓库中实际存在的其他构建入口，人应要求 AI 采用小步修改并立即验证的方式推进，避免大批量改动后再集中排错。
- 若 AI 生成了寄存器配置、时序等待或硬件假设，人应默认先审查其来源是否明确，再决定是否接收。
- 没有来源支撑的内容，应回退到待确认状态。

### 9. 模块接入与复用流程

新增一个模块时，推荐按以下步骤处理。

1. 明确它属于 `app`、`middleware`、`device` 还是 `platform`。
2. 先确定接口边界，再写代码。
3. 若是器件模块，优先先拆驱动主体，再做平台绑定，最后接到上层调用。
4. 新建对应目录和文件，不要先塞进 `main.c` 或生成文件自动区。
5. 接入 CubeMX 调用入口或 `app` 调度入口。
6. 同步当前有效构建入口。当前主流程下先接入 Keil 编译组；若后续补回 CMake，再同步其源文件列表。
7. 通过 Keil 下载到板上，再用串口、日志、上位机或状态灯做最小联调验证。
8. 同步补充 `docs/` 中的使用说明或调试记录。

### 10. 为什么这样分层

这套结构是为了降低下面这些问题。

- 新增外设时，`main.c` 越改越乱。
- 换一个 MCU 后，驱动和业务全部重写。
- CubeMX 重新生成后，用户逻辑被冲掉。
- 团队成员接手工程时，不知道文件应该放哪里。
- 业务、驱动、调试打印互相缠绕，最后无法复用。

分层的目的，是让变化停在该停的边界。

### 11. 项目演进建议

当前模板工程后续可以继续朝以下方向演进。

1. **静态内存策略**：为中间件提供 `create_static` 或等价接口，减少对动态内存的依赖。
2. **事件驱动或消息分发**：把采集、显示、上报拆成更清楚的事件流。
3. **状态机建模**：把连接、重连、初始化、异常恢复等流程抽象成明确状态机。
4. **可测试架构**：在 PC 端或仿真环境下做接口 mock，验证逻辑而非只依赖硬件联调。
5. **配置版本迁移**：给配置结构增加版本号与向后兼容策略。
6. **统一错误码体系**：形成模块化错误码与统一日志上下文。
7. **RTOS 化调度**：在需要时把阻塞轮询改为任务或队列模型。

这些内容都属于长期可维护工程常见的演进方向。

### 12. 常见不合理做法

以下做法视为项目结构不合理。

- 所有代码都堆在 `main.c`。
- 器件驱动里直接调用 HAL SPI 或 HAL I2C。
- 业务层直接修改寄存器。
- 中间件里写具体板级 GPIO 控制。
- 重新生成 CubeMX 后靠手工回忆补代码。
- 目录结构写的是一套，实际工程落地是另一套。
- 把 `build/`、编译中间文件、缓存、日志或编辑器用户配置提交进仓库。
- 为了减小仓库体积，直接移除 `Drivers/`、`Middlewares/` 或生成资产，但没有记录来源、版本和恢复路径。

### 13. 提交前自检清单

- [ ] 新增代码是否放在正确目录。
- [ ] `app`、`middleware`、`device`、`platform`、`Core` 的职责是否混层。
- [ ] `device` 是否仍保持平台无关。
- [ ] `platform` 是否只负责平台实现，不夹带业务逻辑。
- [ ] VS Code 中新增或移动的源码、头文件、配置文件，是否已同步到实际构建链路。
- [ ] CubeMX 生成区和用户逻辑区是否分离清楚。
- [ ] 若已维护 Keil 工程，新增源码是否已纳入并可正常编译。
- [ ] 当前有效构建入口是否已同步完整。当前主流程下至少核对 Keil 工程组；若仓库另有 CMake，再同步其源文件列表。
- [ ] `.gitignore` 是否已覆盖 `build/`、编译中间文件、缓存、日志和本地 IDE 用户态文件。
- [ ] 本次提交是否只包含与当前任务相关的文件，没有混入无关自动产物、缓存或误跟踪的大目录改动。
- [ ] 若第三方库或生成资产未纳入版本管理，`docs/` 是否已写清来源、版本、获取方式和重生成路径。
- [ ] 联调临时代码是否已按计划清理或显式开关化。
- [ ] 若改动涉及 FreeRTOS、初始化时序、中断或任务切换，是否已做过至少一次板上现象观察，而不是只停在静态检查。
- [ ] 若依赖 AI 协助，任务边界、验证方式和不可改区域是否已提前说明，并且关键事实已先从文档、现有代码或手册中搜索确认。
- [ ] 文档是否与当前工程组织方式一致。

### 14. 如何更新这份规范

后续更新按下面几条处理。

1. 只吸收项目级共性问题。单个器件的寄存器、时序、协议细节，优先写入 `docs/ic开发规范.md`。
2. 先修正文档歧义，再新增条目。若已有内容能合并改写，不继续往后追加重复段落。
3. 工具链和目录边界变化后，同步检查 `.github/copilot-instructions.md` 与 `.github/instructions/*.instructions.md` 是否仍然指向正确入口。
4. 版本段只保留关键里程碑。同一阶段的连续收口，可以合并成一条版本记录。措辞微调、去重、格式整理不必展开成流水账。
5. 写入本文件的新规则，要能直接指导实现或复查，避免只留原则口号。
6. 每次有效修改后，同步更新文末的当前版本和最后修改时间。

### 14.1 回答和写作风格

- 在回答时，尽量避免使用“引号 + A-B-C”的表达方式。
- 除非必要，减少冒号、破折号、分号。
- 避免使用超长复合句。
- 尽可能避免使用“链路”“回路”“闭环”“耦合”等复杂、生涩词汇。
- 减少关联词和过渡词的使用。
- 尤其避免在一段里反复重复同一类否定句。少重复用“不是”。
- 句子里的过渡词和连接词，尽量使用最基础、最常用的词语。
- 在做总结、框架、步骤等任务时，可以适当使用分列表和小标题，让信息更清楚。
- 绝对避免超级长难句。
- 绝对避免输出类似这种句子：“我直接给你最快、最稳、一步到位的解决方法，按顺序做就能 100% 解决”，有8个字一样就不行。绝对避免这种过度承诺式的表达。
- 绝对避免含义类似的话重复讲很多遍，可以从不同方面讲述或者简单提及。
- 句子风格可以有变化，不必强行对齐。
- 不推荐使用否定句来表达边界。更推荐直接说“只能做什么，不能做什么”。
- 推荐直接使用标题后面直接跟上正文的格式。不推荐在正文和和标题单独冒出一句话。
- 整理本文件前半部分时，也要严格遵守以上要求。
- 写给人的规范要像团队规范，不要写成提示词。C 语言风格草案尤其要注意这一点。

评价标准来源 ： 1. Guidelines for Reliable RTOS Usage — Embedded Computing Design
  2. FreeRTOS Implementation Quality Management — FreeRTOS.org
  3. Security Overview — FreeRTOS.org
  4. Evaluating Reliability against SEE of Embedded Systems — Bosio et al., 2022 / De Sio et al., 2023
  5. review-firmware Agent Skill Checklist — SkillsMP
  6. eRCaGuy_hello_world — Embedded Architecture & Best Practices
  7. Watchdog Timer Anti-patterns — EmbeddedRelated
  8. Firmware Hardening: Watchdog, Test Coverage, Protocol Contracts — Hubble Community
  9. MISRA-C Compliance Checking — Beningo Embedded
  10. ECLAIR MISRA-C:2012 + HIS Source Code Metrics — BUGSENG
  11. Rules for Writing Embedded Software Code — Promwad
  12. Embedded Coding Standards 2026 — Promwad
  
### 15. 版本记录

- v3.12（2026-05-13）：将主工作流程调整为 CubeMX + VS Code + Keil，补充 FreeRTOS 场景下的板级验证要求。补充项目级验证策略、验证顺序和按改动类型选择最小验证动作的短模板。
- v3.9（2026-05-03）：重排文档结构，补充 C 语言编写风格草案，统一前半部分表述。
- v3.8（2026-05-01）：补充 Git 版本管理约束，并同步仓库级 AI 指令入口。
- v3.7（2026-03-22）：整合新建工程起步阶段的人机协作约束，统一为“人看/人做”和“AI 读取”两部分；补充先搜索确认、仍不明确则提问或标记待确认的规则，并同步对齐仓库级 AI 指令、自检项和 AI 最小输入集维护要求。
- v3.5（2026-03-22）：补全项目级规范的工作流约束，包括 VS Code 修改边界、联调/测试代码收口策略，以及规范更新与 AI 自动加载规则同步维护要求。
- v3.2（2026-03-22）：合并原说明文档内容，统一项目级规范文件名为 `docs/嵌软开发规范.md`，明确与 IC 规范的分工，并补充分层理解、接口抽象、驱动复用与项目演进建议。
- v3.1（2026-03-21）：补充联调临时代码收口约束，并在提交前清单中增加 CMake/Keil 双构建同步与临时代码清理检查项。
- v3.0（2026-03-20）：初次按项目级规范重写，明确本文件定位，结合 CubeMX + Keil 工作流重构目录结构与分层职责。